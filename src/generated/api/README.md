# API Types

Tipos gerados a partir de `docs/openapi/flag-redis.openapi.json`.

Use:

```bash
npm run generate:api-types
```

O arquivo `schema.d.ts` é gerado automaticamente e não deve receber edição manual.
