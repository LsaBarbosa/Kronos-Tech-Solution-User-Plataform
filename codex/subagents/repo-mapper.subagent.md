# Subagent — Repo Mapper

## Objetivo

Mapear o estado atual da rota `/criar-colaborador` no front-end, back-end e documentação.

## Tarefas

1. Confirmar rota em `src/config/app-routes.ts`.
2. Confirmar lazy import em `src/App.tsx`.
3. Abrir `src/pages/CriarColaborador.tsx`.
4. Abrir `src/hooks/useCreateCollaborator.ts`.
5. Abrir `src/service/collaborator-management.service.ts`.
6. Abrir `src/config/api-routes.ts`.
7. Abrir componentes estruturais usados nas telas novas.
8. Registrar dependências e pontos de remoção do legado.

## Saída esperada

Relatório curto com:

- arquivos que serão alterados;
- arquivos que serão criados;
- arquivos que devem permanecer intactos;
- riscos técnicos identificados.
